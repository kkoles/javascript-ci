name: JS CI

on:
  workflow_dispatch:

jobs:
  build:
    uses: kkoles/workflows-examples/.github/workflows/js.yml@main
    secrets:
      GH_READONLY_TOKEN: ${{ secrets.GH_READONLY_TOKEN }}
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}